import WaitFor from "framework/WaitFor";
const wait = require("wait.for");
import logger from "framework/Logger";;
import {up as UserDataUp} from "migrations/UserData";
import {upSync as SyncDataUp} from "migrations/SyncData";

export default function MigrationRunner(result) {
    wait.launchFiber(() => {
        WaitFor(SyncDataUp());
        WaitFor(UserDataUp());
        console.log(result, "!!!!");
    });
}
