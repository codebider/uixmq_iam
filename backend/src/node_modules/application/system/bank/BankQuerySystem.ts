import {BankGateway, MakeBankGateway} from "gateway/bank/BankGateway";
import {CreateBank, CreateBankData, Bank, BankImpl} from "domain/bank/Bank";

export function MakeBankQuerySystem(gateway?: BankGateway) {
    return new BankQuerySystem(gateway || MakeBankGateway());
}

class BankQuerySystem {
    private gateway: BankGateway;

    constructor(gateway: BankGateway) {
        this.gateway = gateway;
    }

    public findAllBank() {
        return null;
    }
}
