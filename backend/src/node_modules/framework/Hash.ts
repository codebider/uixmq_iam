const md5 = require('md5');
const key = process.env.HASH_KEY || '';

interface HashProperties {
    hash: (msg: string) => string;
}

export const Hash: HashProperties = {
    hash: (msg: string) => {
        return md5(msg + '');
    }
}
